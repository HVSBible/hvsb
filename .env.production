# Don't use this file for production keys because these keys will be used whenever an app is built and deployed which includes both dev and production deployments. We store production keys as environment variables in Vercel so that we can control when they are used (just for production deploys from the main branch).